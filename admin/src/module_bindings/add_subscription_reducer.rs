// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit ).

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddSubscriptionArgs {
    pub subscriber_account_id: u64,
    pub subscriber_email: String,
    pub category_id: u64,
}

impl From<AddSubscriptionArgs> for super::Reducer {
    fn from(args: AddSubscriptionArgs) -> Self {
        Self::AddSubscription {
            subscriber_account_id: args.subscriber_account_id,
            subscriber_email: args.subscriber_email,
            category_id: args.category_id,
        }
    }
}

impl __sdk::InModule for AddSubscriptionArgs {
    type Module = super::RemoteModule;
}

pub struct AddSubscriptionCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_subscription`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_subscription {
    /// Request that the remote module invoke the reducer `add_subscription` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_add_subscription`] callbacks.
    fn add_subscription(
        &self,
        subscriber_account_id: u64,
        subscriber_email: String,
        category_id: u64,
    ) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `add_subscription`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`AddSubscriptionCallbackId`] can be passed to [`Self::remove_on_add_subscription`]
    /// to cancel the callback.
    fn on_add_subscription(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &u64, &String, &u64) + Send + 'static,
    ) -> AddSubscriptionCallbackId;
    /// Cancel a callback previously registered by [`Self::on_add_subscription`],
    /// causing it not to run in the future.
    fn remove_on_add_subscription(&self, callback: AddSubscriptionCallbackId);
}

impl add_subscription for super::RemoteReducers {
    fn add_subscription(
        &self,
        subscriber_account_id: u64,
        subscriber_email: String,
        category_id: u64,
    ) -> __sdk::Result<()> {
        self.imp.call_reducer(
            "add_subscription",
            AddSubscriptionArgs {
                subscriber_account_id,
                subscriber_email,
                category_id,
            },
        )
    }
    fn on_add_subscription(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &u64, &String, &u64) + Send + 'static,
    ) -> AddSubscriptionCallbackId {
        AddSubscriptionCallbackId(self.imp.on_reducer(
            "add_subscription",
            Box::new(move |ctx: &super::ReducerEventContext| {
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer:
                                super::Reducer::AddSubscription {
                                    subscriber_account_id,
                                    subscriber_email,
                                    category_id,
                                },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, subscriber_account_id, subscriber_email, category_id)
            }),
        ))
    }
    fn remove_on_add_subscription(&self, callback: AddSubscriptionCallbackId) {
        self.imp.remove_on_reducer("add_subscription", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `add_subscription`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_add_subscription {
    /// Set the call-reducer flags for the reducer `add_subscription` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn add_subscription(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_add_subscription for super::SetReducerFlags {
    fn add_subscription(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("add_subscription", flags);
    }
}
